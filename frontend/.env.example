# Frontend Environment Variables
# Backend API & WebSocket URLs
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080

# (Optional) Explicit Public App URL override for custom domains or tunnels (defaults dynamically to window.location.origin)
# VITE_PUBLIC_APP_URL=https://your-app.vercel.app
